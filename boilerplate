const morgan = require('morgan')
const express = require('express')
const port = 8000;

const app = express()
app.use(morgan('dev'))


app.listen(port, () => {
    console.log(`listening on port ${port}`)
})
